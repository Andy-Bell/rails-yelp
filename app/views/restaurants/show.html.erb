<h1> <%= @restaurant.name %> </h1>
<p><%= @restaurant.description %></p>

<p><%= link_to "Delete #{@restaurant.name}", restaurant_path(@restaurant), method: :delete %></p>
<p><%= link_to "Edit #{@restaurant.name}", edit_restaurant_path(@restaurant)%></p>

<p>
  <%= link_to "Review #{@restaurant.name}", new_restaurant_review_path(@restaurant) %>
</p>

Average rating: <%= star_rating(@restaurant.average_review_score) %>
<ul id='reviews'>
  <% @restaurant.reviews.each do |review| %>
    <li>
      Rating = <%= review.rating %>/5
      <%= review.comments %>
      Review added by = <%= User.find(review.user_id).email %>
    </li>
  <% end %>
</ul>
